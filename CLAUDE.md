# CLAUDE.md — 給 AI 的指令層

> 這個檔是**指令**，不是筆記。凡是要 AI 無條件執行的規則寫在這裡；
> 知識、事實、進度寫在 `Claude_背景.md` 與各層資料夾（那些對 AI 只是背景參考）。

## Session 開場（無條件執行）

每個新對話第一件事：

1. 先讀 `Claude_背景.md`（入口檔，一頁）
2. 再依當前任務讀對應的 `40_Workflows/` 專案檔
3. 需要查數字才讀 `50_資料庫/入口總表.md` 選資料源
4. **讀完才開工**，不要憑印象回答

## 觸發詞

**「更新Obsidian」** = 把本次 session 的新事實／決策／進度寫回：

- 對應的 `40_Workflows/` 專案檔（進度、待辦、拍板紀錄）
- `Claude_背景.md`（只更新該專案那一句現況，維持一頁）
- 若有新的跨案事實 → `80_References/`
- 若有新的資料表 → 同時更新 `50_資料庫/入口總表.md`

寫完後 `git commit`，**明確列出動過的檔名，禁止 `git add -A`**。

## 下令三原則

1. **先提案 → 我拍板 → 再動手。** 梳理、搬檔、刪檔、大範圍改寫，一律先給方案等確認。
2. **要淘汰的檔移進 `_archive/`，不真刪。**
3. **`_private/` 不要碰、不要讀進對話、不要寫進 commit 訊息。**

## 這個 vault 的性質（重要）

這裡是**個人投資決策紀錄**，不是投資建議來源。AI 協作時：

- 數字一律以 `50_資料庫/` 的表為準，**不要自己回想或推估股價、EPS、殖利率**
- 表裡標「待查」的欄位就是還沒查證，不要當成已知事實
- 提到個股結論時，標明是根據哪一份檔案的哪一次快照（資料有日期，會過期）
- 這裡所有評等與建議都是**當時的個人判讀**，不是專業投資建議

## 分級（分享前必看）

| 等級 | 範圍 | 內容 |
|---|---|---|
| private | 只有自己 | `_private/`（私人照片）、`50_資料庫/friend_*`（朋友的完整持股與金額） |
| shared | 可分享 | `80_References/` 的公開資料整理（財報、官股金控對照） |

**整包 vault 不要直接給別人或別人的 AI 讀**——朋友的持股明細在裡面。

---

## 給本機使用者：複製到 ~/.claude/CLAUDE.md

若你在自己電腦上用 Claude Code 開這個 repo，本檔會自動被讀取，不必額外設定。
若你想在**任何目錄**都生效，把上面「Session 開場」與「觸發詞」兩段複製進
`~/.claude/CLAUDE.md`，並把路徑改成 vault 的實際絕對路徑，例如：

```
每個新對話第一件事：先讀 C:\<你的vault路徑>\Claude_背景.md，
再依當前任務讀對應 40_Workflows\ 專案檔，讀完才開工。
```
